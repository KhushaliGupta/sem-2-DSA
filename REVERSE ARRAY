/# include <stdio.h>
void main()
{
    int n;
    printf("Enter the value of n:");
    scanf("%d",& n);
    int arr[n],i;
    for(i=0;i<=n-1;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=n-1;i>=0;i--)
    {
        printf(" %d ",arr[i]);
    }
}
